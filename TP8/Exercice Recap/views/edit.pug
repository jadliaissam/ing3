<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body
        h1 Edition du produit 
        form(action="/products/:id/edit",method="post")
            div
                label id produit 
                input(type="number", name="id", value=product.id)
            div
                label nom produit 
                input(type="text", name="title",value=product.title)
            div    
                label prix produit 
                input(type="number", name="prix", value=product.prix)
            input(type="submit", text="modifier")